import { Grid } from "@mui/material";
import React from "react";

type Props = {};

export const MyFunction = (props: Props) => {
  return (
    <Grid item xs={8}>
      MyFunction
    </Grid>
  );
};
